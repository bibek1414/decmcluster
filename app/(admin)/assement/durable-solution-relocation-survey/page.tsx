import AssessmentDetailClient from "@/components/(admin)/assessment/details/assessment-detail-client";

export const metadata = {
  title: "Durable Solution & Relocation Survey | Decmcluster",
  description: "Field name database table and schema management.",
};

export default function DurableSolutionRelocationSurveyPage() {
  return <AssessmentDetailClient slug="durable-solution-relocation-survey" />;
}
