import { ToolsClient } from "@/components/(admin)/tools/tools-client";

export const metadata = {
  title: "Assessment Tools | Decmcluster",
  description: "Create, configure, and manage dynamic data collection tools and survey assessment forms.",
};

export default function AssessmentToolsPage() {
  return <ToolsClient />;
}
